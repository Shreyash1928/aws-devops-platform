- name: Configure application servers
  hosts: all
  connection: community.aws.aws_ssm
  gather_facts: false
  become: false

  tasks:
    - name: Test SSM connectivity
      ansible.builtin.command: echo "SSM CONNECTED"
