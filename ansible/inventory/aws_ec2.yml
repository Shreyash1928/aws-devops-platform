plugin: amazon.aws.aws_ec2

regions:
  - ap-south-1

filters:
  tag:Environment: dev

hostnames:
  - instance-id

compose:
  ansible_connection: amazon.aws.aws_ssm
  ansible_aws_ssm_region: ap-south-1
